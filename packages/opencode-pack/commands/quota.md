Show the free-tier quota status of every failover provider behind the omni-forge router.

## Usage
- `quota` → table of each provider: health, requests today / daily cap, cooldown remaining

## Outcome
A markdown table you can read at a glance, plus a recommendation of which free tier to lean on next.