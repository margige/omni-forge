Speak text aloud with omni-forge. Invokes `forge_tts` with the given text.

## Usage
- `tts Good morning, pilot. Weather is clear.` → mp3 with the default voice

## Outcome
Returns the speech file URL served by the omni-forge core.