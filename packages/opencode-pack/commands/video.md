Create a short video with omni-forge. Submits a job to the Wan 2.1 backend and watches it until completion.

## Usage
- `video a drone shot flying over a misty forest` → starts a video job

## Outcome
Returns the rendered video URL. Requires `FORGE_WAN_COMMAND` to be configured.